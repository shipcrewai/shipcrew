[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "shipply_gate_2"
version = "0.1.0"
description = "Bot handler for shipply-gate-2"
readme = "README.md"
license = { text = "MIT OR Apache-2.0" }
requires-python = ">=3.10"
dependencies = [
    "pacto-bot-sdk>=0.2.0",


]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.hatch.build]
include = [
    "README.md",
    "src/shipply_gate_2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/shipply_gate_2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
